name: Dummy-golangci-lint
on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - main

jobs:
  golangci:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - name: Exit with success
        run: exit 0
